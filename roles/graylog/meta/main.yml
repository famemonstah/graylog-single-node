---
galaxy_info:
  author: "giga maglakelidze"
  description: "Install and configure Graylog 7.0 with MongoDB 8.0 and embedded OpenSearch via graylog-datanode (single-node)"
  license: "Apache-2.0"
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - noble       # 24.04
  galaxy_tags:
    - graylog
    - logging
    - mongodb
    - monitoring

dependencies: []
